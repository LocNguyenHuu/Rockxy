// LocalDev.xcconfig — Local development configuration
// Uses release identity (same as staging/production) so dev builds
// can still communicate with the helper without full identity isolation.

#include "Base.xcconfig"

ROCKXY_RELEASE_CHANNEL = local-dev
ROCKXY_UPDATES_ENABLED = NO
